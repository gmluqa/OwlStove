const { expect } = require("chai");

// run npx hardhat test to run all the tests

describe("OwlStove contract", function () {
  it("Only allows 1 mint per address", async function () {});
  it("Only allows for 33 mints", async function () {});
});
